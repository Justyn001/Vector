#include <gtest/gtest.h>
#include "Wektor.h"



/*
TEST(W, K)
{
	Wektor<int> vector;
	EXPECT_EQ(1, vector.getcapacity());
}


TEST(W, KA)
{
	Wektor<int> vector(10);
	EXPECT_EQ(10, vector.getcapacity());
	EXPECT_EQ(0, vector.getcurrent());
}

TEST(W, KW)
{
	Wektor<int> test(10);
	Wektor<int> vector(test);
	EXPECT_EQ(10, vector.getcapacity());
	EXPECT_EQ(0, vector.getcurrent());
}

TEST(W, PB)
{
	Wektor<int> vector;
	vector.push_back(10);
	EXPECT_EQ(1, vector.getcapacity());
	EXPECT_EQ(1, vector.getcurrent());
}

TEST(W, AS)
{
	Wektor<int> vector;
	vector.assign(1,2);
	EXPECT_EQ(1, vector.getcapacity());
	EXPECT_EQ(1, vector.getcurrent());
}
*/